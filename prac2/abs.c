#include <stdio.h>
#include "main.h"

/**
 * _abs: Code obtains the absolute value of an integer
 *
 * return: absolute value
 */

int _abs(int a)
{
	if (a < 0)
	{
		return (-(a));
	}
	if (a > 0)
	{
		return a;
	}
	else
	{
		return a;
	}
}
