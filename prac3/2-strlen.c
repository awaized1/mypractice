#include <stdio.h>
#include "main.h"

/**
 * _strlen - Code returns the length of a string
 *
 * return - Length of string
 */

int _strlen(char *s)
{
	int a;
	for (a = 0; s[a] != '\0'; a++)
	{
	}
	return a;
}
